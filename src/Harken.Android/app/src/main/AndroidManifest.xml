<?xml version="1.0" encoding="utf-8"?>
<!--
  allowBackup is false, deliberately and not by omission.

  ADR-0011 put transcription on the device and the product says, in onboarding and on the
  recording notification, that nothing recorded leaves the phone. Auto Backup with no
  extraction rules copies the whole of filesDir and databases/ to Google Drive and hands
  the same set to device-to-device transfer — that is every WAV and every transcript.
  Rules excluding those two directories would also work, but they are two files (v31+ and
  v26-30 are separate) and they fail open: a new data directory added later is backed up
  until someone remembers to exclude it. Off is the claim the app actually makes, and the
  user's own copy is ARC-033's explicit export rather than a silent upload.

  usesCleartextTraffic is likewise absent rather than false. It was true for the Retrofit
  backend ADR-0011 removed; the only destination left is an https:// GitHub release URL,
  and at this targetSdk the platform default already refuses cleartext.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".HarkenApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Harken">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".recording.RecordingForegroundService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- dataSync, not microphone: this one decodes a file that already exists. Without
             it a transcription ran in a cached process holding ~610 MB and Android
             reclaimed it mid-decode (ARC-003). -->
        <service
            android:name=".speech.TranscriptionService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

    </application>
</manifest>
